{% extends 'base.html.twig' %}

{% block title %}Cv{% endblock %}

{% block body %}
<div class="card">
     <div class="card-header bg-info">
    <h1>Details de mon CV</h1>
</div>
</div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cv.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ cv.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ cv.age }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cv.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ cv.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cv.adresse }}</td>
            </tr>
            <tr>
                <th>Specialite</th>
                <td>{{ cv.specialite }}</td>
            </tr>
            <tr>
                <th>NiveauEtude</th>
                <td>{{ cv.niveauEtude }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ cv.experience }}</td>
            </tr>
        </tbody>
    </table>

    <a class ="btn btn-sm btn-primary" href="{{ path('cv_index') }}">Retour à la liste</a>
    <a  class ="btn btn-sm btn-primary" href="{{ path('cv_edit', {'id': cv.id}) }}">Modifier</a>

    {# {{ include('cv/_delete_form.html.twig') }} #}
{% endblock %}
