{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="card">
     <div class="card-header bg-info">
    <h1>Voir l'utilisateur</h1>
</div>
     </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ user.login }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}" class="btn btn-sm btn-primary" >back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-sm btn-primary">edit</a>

    {# {{ include('user/_delete_form.html.twig') }} #}
{% endblock %}
